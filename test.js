/*
function fourLetters(names) {
    let countOfNames = 0;
    slicedNames = names.split(" ");   
    for (let name of slicedNames) {
        if (name.length == 4) 
        countOfNames ++;
    }
    return countOfNames;
     
}

console.log(fourLetters("Tror Gvigris Deriana Nori"));*/

/**
 * Реализуйте функцию getTotalTime
 */
function getTotalTime(heroes, n) {
    
}